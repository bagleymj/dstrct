class Post < ActiveRecord::Base
  belongs_to :user
  before_save :default_values

  def default_values
    self.score ||= 0
    self.lifespan ||= 3600
    self.destruct_at = 3600.seconds.from_now
  end

  def reset_score
    self.score = 0
    self.save
  end
end
